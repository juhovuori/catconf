To install from scratch:

1. Install and start couchdb (>= 1.3 for CORS support)
2. Create a couchdb admin with password
3. cp conf.py.dist to conf.py
4. Edit conf.py to reflect your couchdb settings
5. ./manage.py install

